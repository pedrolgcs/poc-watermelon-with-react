import React from 'react';
import ReactDOM from 'react-dom';
import App from './App';
import AppLoki from './AppLoki';

ReactDOM.render(
  <React.StrictMode>
    <AppLoki />
  </React.StrictMode>,
  document.getElementById('root')
);
